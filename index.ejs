<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@100..900&family=Quattrocento:wght@400;700&display=swap" rel="stylesheet">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="/css/common.css">


</head>

<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="index.html">
                <img src="<%= logoPath %>" alt="logo">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- Center-aligned navbar links -->
                <ul class="navbar-nav mx-auto">
                    <% navLinks.forEach(function(link) { %>
                        <li class="nav-item"><a class="nav-link" href="<%= link.url %>"><%= link.text %></a></li>
                    <% }); %>
                </ul>
                <!-- Right-aligned buttons -->
                <div class="d-flex">
                    <% buttons.forEach(function(button) { %>
                        <a href="<%= button.url %>" class="btn btn-custom <%= button.class %>"><%= button.text %></a>
                    <% }); %>
                </div>
            </div>
        </div>
    </nav>
    
    <div class="video-container">
        <iframe class="responsive-video" src="<%= videoUrl %>" frameborder="0" allow="autoplay; fullscreen" allowfullscreen></iframe>
    </div>

    <div class="hospitality-section text-center">
        <h1 class="title"><%= heroTitle %></h1>
        <div class="line"></div>
        <p class="subtitle"><%= heroSubtitle %></p>
    </div>
    
    <br>
    <br>
    <br>

    <div class="container text-center">
        <!-- First Row -->
        <div class="row mb-4">
            <% firstRowImages.forEach(function(image) { %>
                <div class="col">
                    <img src="<%= image.src %>" class="img-fluid rounded" alt="<%= image.alt %>">
                    <h5 class="mt-3" style="font-family: 'Quattrocento', serif; color: #555; line-height: 1.6;">
                        <%= image.description %>
                    </h5>
                </div>
            <% }); %>
        </div>
    
        <br>
        <br><br>
        
        <div class="features-section section">
            <div class="container">
                <h2 class="text-center"><%= featuresTitle %></h2>
                <div class="row">
                    <% features.forEach(function(feature) { %>
                        <div class="col-md-4 feature">
                            <i class="bi <%= feature.icon %>"></i>
                            <h5><%= feature.title %></h5>
                            <p><%= feature.description %></p>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div>
        <br>
        <br><br>
        
        <!-- Second Row -->
        <div class="row">
            <% secondRowImages.forEach(function(image) { %>
                <div class="col">
                    <img src="<%= image.src %>" class="img-fluid rounded" alt="<%= image.alt %>">
                    <h5 class="mt-3" style="font-family: 'Quattrocento', serif; color: #555; line-height: 1.6;">
                        <%= image.description %>
                    </h5>
                </div>
            <% }); %>
        </div>
    </div>

    <footer class="footer mt-5">
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <h5>About Vistora</h5>
                <p>Experience luxury and comfort in the heart of the city at Vistora Hotels.</p>
            </div>
            <div class="col-md-3">
                <h5>Quick Links</h5>
                <ul class="list-unstyled">
                    <li><a href="/">Home</a></li>
                    <li><a href="/rooms">Rooms & Suites</a></li>
                    <li><a href="/dining">Dining</a></li>
                    <li><a href="/spa">Spa & Wellness</a></li>
                </ul>
            </div>
            <div class="col-md-3">
                <h5>Contact</h5>
                <p>Email: support@vistora.com</p>
                <p>Phone: +1 800 987 6543</p>
            </div>
            <div class="col-md-3">
                <h5>Follow Us</h5>
                <a href="#"><i class="bi bi-facebook me-2"></i></a>
                <a href="#"><i class="bi bi-twitter me-2"></i></a>
                <a href="#"><i class="bi bi-instagram"></i></a>
            </div>
        </div>
        <div class="footer-bottom mt-4">
            <p>&copy; 2024 Vistora. All rights reserved.</p>
            <ul class="list-inline">
                <li class="list-inline-item"><a href="#">Privacy Policy</a></li>
                <li class="list-inline-item"><a href="#">Terms of Use</a></li>
            </ul>
        </div>
    </div>
</footer>
    <!-- Bootstrap Bundle with Popper (includes Popper.js) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
integrity="sha384-pBW+Enm6b9qrwh2i4Y9RxlThVb4n+zj1nM+v3oYeclhvl3rAcw0Zg5q/0sfZ9M6I"
crossorigin="anonymous"></script>

</body>

</html>
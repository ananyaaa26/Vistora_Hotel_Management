<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@100..900&family=Quattrocento:wght@400;700&display=swap" rel="stylesheet">
    <title><%= title %></title>
    <style>
        body {
            background-color: #fff;
            font-family: "Quattrocento", serif;
            margin: 0;
            padding: 0;
        }

        .navbar {
            background-color: #fff;
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 10px 0;
        }

        .navbar-brand img {
            max-width: 150px;
            height: auto;
        }

        .navbar-nav .nav-link {
            color: #000;
            padding: 0 15px;
            font-size: 1rem;
            font-weight: 500;
        }

        .navbar-nav .nav-link:hover {
            color: #bb9e44;
        }

        .btn-custom {
            color: #fff;
            background-color: #bb9e44;
            border-radius: 50px;
            padding: 8px 20px;
            font-size: 1rem;
            border: none;
        }

        .btn-custom:hover {
            background-color: #9d842e;
        }

        iframe.video-frame {
            width: 100%;
            height: 400px;
            border: none;
        }

        .footer {
            background-color: #222;
            color: #ccc;
            padding: 40px 0;
        }

        .footer h5 {
            color: #fff;
            margin-bottom: 20px;
            font-size: 1.2rem;
        }

        .footer a {
            color: #ccc;
            text-decoration: none;
            font-size: 1rem;
        }

        .footer a:hover {
            color: #fff;
            text-decoration: underline;
        }

        .footer-bottom {
            padding-top: 20px;
            border-top: 1px solid #444;
            color: #888;
            text-align: center;
        }

        .form-section {
            padding: 40px 0;
        }

        .form-section h2 {
            font-size: 2rem;
            margin-bottom: 20px;
            font-weight: 700;
        }

        .form-control {
            border-radius: 0.5rem;
        }

        .btn-submit {
            background-color: #bb9e44;
            color: #fff;
            border-radius: 50px;
            padding: 10px 20px;
            font-size: 1rem;
            border: none;
        }

        .btn-submit:hover {
            background-color: #9d842e;
        }

        .info-section {
            background-color: #f8f9fa;
            padding: 40px 20px;
        }

        .info-section h3 {
            font-size: 1.5rem;
            margin-bottom: 20px;
        }

        .info-section .info-item {
            margin-bottom: 20px;
        }

        .info-section .info-item i {
            color: #bb9e44;
            font-size: 1.5rem;
            margin-right: 15px;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #fff; border-bottom: 1px solid #ddd;">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src="<%= logoPath %>" alt="logo" class="d-inline-block align-text-top" style="height: 40px;">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <% navLinks.forEach(function(link) { %>
                        <li class="nav-item">
                            <a class="nav-link text-uppercase fw-bold" href="<%= link.url %>" style="color: #333; font-family: 'Quattrocento', serif;"><%= link.text %></a>
                        </li>
                    <% }); %>
                </ul>
                <div class="d-flex">
                    <a href="/booking" class="btn" style="background-color: #caa945; color: #fff; border-radius: 20px; font-family: 'Quattrocento', serif; font-weight: bold; margin-left: 10px;">BOOK NOW</a>
                    <% if (locals.isAuthenticated) { %>
                        <a href="/api/logout" class="btn" style="background-color: #caa945; color: #fff; border-radius: 20px; font-family: 'Quattrocento', serif; font-weight: bold; margin-left: 10px;">LOGOUT</a>
                    <% } else { %>
                        <a href="/login" class="btn" style="background-color: #caa945; color: #fff; border-radius: 20px; font-family: 'Quattrocento', serif; font-weight: bold; margin-left: 10px;">LOGIN</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <br><br><br><br>
    <section class="form-section">
        <div class="container">
            <div class="row">
                <div class="col-lg-6">
                    <h2>We'd Love to Hear From You</h2>
                    <p>Whether you have questions, feedback, or require assistance, our team is here to help. Fill out the form below, and we'll get back to you as soon as possible.</p>
                    <form action="/send-email" method="post">
                        <div class="mb-3">
                            <label for="name" class="form-label">Your Name</label>
                            <input type="text" class="form-control" id="name" name="name" placeholder="Enter your name" required>
                        </div>
                        <div class="mb-3">
                            <label for="email" class="form-label">Your Email</label>
                            <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
                        </div>
                        <div class="mb-3">
                            <label for="phone" class="form-label">Phone Number</label>
                            <input type="tel" class="form-control" id="phone" name="phone" placeholder="Enter your phone number">
                        </div>
                        <div class="mb-3">
                            <label for="message" class="form-label">Your Message</label>
                            <textarea class="form-control" id="message" name="message" rows="5" placeholder="Type your message here..." required></textarea>
                        </div>
                        <button type="submit" class="btn btn-submit">Send Message</button>
                    </form>
                </div>
                <div class="col-lg-6 info-section">
                    <h3>Contact Information</h3>
                    <div class="info-item">
                        <i class="bi bi-geo-alt-fill"></i>
                        <span>123 Luxury St, Downtown, Metropolis, USA</span>
                    </div>
                    <div class="info-item">
                        <i class="bi bi-telephone-fill"></i>
                        <span>+1 800 987 6543</span>
                    </div>
                    <div class="info-item">
                        <i class="bi bi-envelope-fill"></i>
                        <span>support@vistora.com</span>
                    </div>
                    <div class="info-item">
                        <i class="bi bi-clock-fill"></i>
                        <span>Monday - Friday: 9:00 AM - 6:00 PM</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer class="footer mt-5">
        <div class="container">
            <div class="row">
                <% footerSections.forEach(function(section) { %>
                    <div class="col-md-3">
                        <h5><%= section.title %></h5>
                        <% if (section.type === 'text') { %>
                            <p><%= section.content %></p>
                        <% } else if (section.type === 'links') { %>
                            <ul class="list-unstyled">
                                <% section.links.forEach(function(link) { %>
                                    <li><a href="<%= link.url %>"><%= link.text %></a></li>
                                <% }); %>
                            </ul>
                        <% } else if (section.type === 'contact') { %>
                            <% section.items.forEach(function(item) { %>
                                <p><%= item %></p>
                            <% }); %>
                        <% } else if (section.type === 'social') { %>
                            <% section.icons.forEach(function(icon) { %>
                                <a href="<%= icon.url %>"><i class="bi <%= icon.class %> me-2"></i></a>
                            <% }); %>
                        <% } %>
                    </div>
                <% }); %>
            </div>
            <div class="footer-bottom mt-4">
                <p>&copy; <%= currentYear %> <%= companyName %>. All rights reserved.</p>
                <ul class="list-inline">
                    <% footerLinks.forEach(function(link) { %>
                        <li class="list-inline-item"><a href="<%= link.url %>"><%= link.text %></a></li>
                    <% }); %>
                </ul>
            </div>
        </div>
    </footer>
</body>

</html>
